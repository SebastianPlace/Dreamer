{{yield}}
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapsible-container" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to "goals" class="navbar-brand"}}Dreamer{{/link-to}}
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="collapsible-container">
      <div class="navbar-right navbar-text navbar-profile-group">
        <img src={{session.currentUser.profileImageURL}} class="img-circle">
        {{session.currentUser.displayName}}
      </div>
      <button {{action "signOut"}} class="btn btn-default navbar-btn navbar-right">Sign out</button>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
