{{#if goals}}
  <ul class="nav nav-pills nav-stacked">

    {{#each goals as |goal|}}
      <li>{{#link-to "goals.detail" goal.id}}{{goal.title}}{{/link-to}}</li>
    {{/each}}
  </ul>
{{else}}
  <p class="text-center">You have no goals.</p>
{{/if}}
